# UDPGW Configuration - Server

# Listen on UDP port (use :5555 for custom applications)
LISTEN="0.0.0.0:5555"

# Remote server to forward packets to
REMOTE="127.0.0.1:53"

# Number of worker threads for processing
WORKERS=8

# Session timeout in seconds
TIMEOUT=300

# Enable verbose logging
VERBOSE=false
